services:
  backend:
    image: nongdev/inventory-backend:latest
    restart: always
    networks:
      - web
    env_file:
      - ./backend/.env
    volumes:
      - backend_storage:/var/www/html/storage
      - ./backend/bootstrap/cache:/var/www/html/bootstrap/cache
    depends_on:
      - db
    command: >
      sh -c "chown -R www-data:www-data storage bootstrap/cache &&
             chmod -R 775 storage bootstrap/cache &&
             php-fpm"
    ports:
      - "8000:8000" # <-- Expose backend HTTP port for Caddy to reach
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/test" ]
      interval: 30s
      timeout: 10s
      retries: 5

  frontend:
    build:
      context: ./frontend
    image: nongdev/inventory-frontend:latest
    restart: always
    networks:
      - web

  ai:
    build:
      context: ./inventory_ai
    image: nongdev/inventory-ai:latest
    restart: always
    networks:
      - web
    depends_on:
      - db

  db:
    image: postgres:15
    container_name: inventory_postgres_db
    restart: always
    environment:
      POSTGRES_DB: inventory_management_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - web

networks:
  web:
    external: true

volumes:
  backend_storage:
  postgres_data:
